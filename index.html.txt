<!DOCTYPE html>
<html lang="vi">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>LÚA MEDIA - TELEGRAM SERVICES</title>
    <style>
        :root {
            --bg-color: #0b0f19;
            --card-bg: rgba(25, 32, 50, 0.7);
            --primary-neon: #9d4edd;
            --secondary-neon: #00f2fe;
            --text-color: #ffffff;
            --text-muted: #94a3b8;
        }

        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
        }

        body {
            background-color: var(--bg-color);
            color: var(--text-color);
            line-height: 1.6;
            overflow-x: hidden;
        }

        /* Container chuẩn */
        .container {
            max-width: 1200px;
            margin: 0 auto;
            padding: 20px;
        }

        /* Header / Hero Section */
        .hero {
            text-align: center;
            padding: 60px 20px;
            background: radial-gradient(circle at center, rgba(157, 78, 221, 0.15) 0%, transparent 70%);
        }

        .hero h1 {
            font-size: 3rem;
            font-weight: 800;
            text-transform: uppercase;
            letter-spacing: 2px;
            margin-bottom: 15px;
            color: #fff;
            text-shadow: 0 0 10px var(--primary-neon), 0 0 20px var(--primary-neon);
        }

        .hero p {
            font-size: 1.2rem;
            color: var(--secondary-neon);
            margin-bottom: 30px;
            font-weight: 600;
        }

        /* Nút Liên Hệ To Bự */
        .btn-cta {
            display: inline-block;
            padding: 15px 40px;
            font-size: 1.2rem;
            font-weight: bold;
            color: #fff;
            text-decoration: none;
            background: linear-gradient(45deg, var(--primary-neon), var(--secondary-neon));
            border-radius: 50px;
            box-shadow: 0 0 15px rgba(0, 242, 254, 0.4);
            transition: all 0.3s ease;
            text-transform: uppercase;
        }

        .btn-cta:hover {
            transform: scale(1.05);
            box-shadow: 0 0 25px rgba(157, 78, 221, 0.8);
        }

        /* Grid Dịch Vụ */
        .services-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(280px, 1fr));
            gap: 25px;
            margin-top: 40px;
        }

        .card {
            background: var(--card-bg);
            border: 1px solid rgba(255, 255, 255, 0.05);
            border-radius: 16px;
            padding: 25px;
            backdrop-filter: blur(10px);
            transition: all 0.3s ease;
            position: relative;
            overflow: hidden;
        }

        .card:hover {
            transform: translateY(-5px);
            border-color: var(--secondary-neon);
            box-shadow: 0 5px 20px rgba(0, 242, 254, 0.1);
        }

        .card::before {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            width: 4px;
            height: 100%;
            background: var(--primary-neon);
        }

        .card h3 {
            font-size: 1.4rem;
            margin-bottom: 15px;
            color: var(--secondary-neon);
            display: flex;
            align-items: center;
            gap: 10px;
        }

        .card ul {
            list-style: none;
        }

        .card ul li {
            margin-bottom: 10px;
            color: var(--text-muted);
            display: flex;
            align-items: center;
            gap: 8px;
        }

        .card ul li::before {
            content: '⚡';
            color: var(--primary-neon);
        }

        /* Bảng Giá / Thời gian */
        .pricing-section {
            margin-top: 60px;
            text-align: center;
        }

        .pricing-section h2 {
            font-size: 2rem;
            margin-bottom: 30px;
            color: #fff;
            text-shadow: 0 0 8px var(--secondary-neon);
        }

        .price-container {
            display: flex;
            justify-content: center;
            gap: 20px;
            flex-wrap: wrap;
        }

        .price-box {
            background: var(--card-bg);
            border: 1px solid rgba(157, 78, 221, 0.2);
            padding: 30px;
            border-radius: 12px;
            width: 300px;
        }

        .price-box.active {
            border-color: var(--secondary-neon);
            background: linear-gradient(145deg, rgba(25, 32, 50, 0.9), rgba(11, 15, 25, 0.9));
        }

        .price-box h4 {
            font-size: 1.5rem;
            color: #fff;
            margin-bottom: 10px;
        }

        .price-box .duration {
            font-size: 1.1rem;
            color: var(--secondary-neon);
            font-weight: bold;
            margin-bottom: 15px;
        }

        /* Footer */
        footer {
            text-align: center;
            padding: 40px 20px;
            margin-top: 60px;
            border-top: 1px solid rgba(255, 255, 255, 0.05);
            color: var(--text-muted);
        }

        /* Responsive Mobile */
        @media (max-width: 768px) {
            .hero h1 { font-size: 2rem; }
            .hero p { font-size: 1rem; }
            .btn-cta { width: 100%; text-align: center; }
        }
    </style>
</head>
<body>

    <div class="container">
        
        <!-- Màn hình chính -->
        <section class="hero">
            <h1>QUẢNG CÁO & LÀM BOT</h1>
            <p>VÀO VIỆC NHANH - GỌN - UY TÍN</p>
            <a href="https://t.me/TenVipQua" target="_blank" class="btn-cta">🚀 INBOX: @TenVipQua NGAY</a>
        </section>

        <!-- Danh sách dịch vụ -->
        <section class="services-grid">
            
            <!-- Card 1 -->
            <div class="card">
                <h3>🤖 LÀM BOT & BÁN SRC</h3>
                <ul>
                    <li>Thiết kế BOT theo yêu cầu</li>
                    <li>Bán SOURCE BOT chạy mượt</li>
                    <li>Hỗ trợ cài đặt lên VPS từ A-Z</li>
                </ul>
            </div>

            <!-- Card 2 -->
            <div class="card">
                <h3>📢 QUẢNG CÁO & MEM</h3>
                <ul>
                    <li>Kéo mem Channel/Group target</li>
                    <li>Treo Banner vị trí đẹp</li>
                    <li>Ghim bài quảng cáo giá ưu đãi</li>
                </ul>
            </div>

            <!-- Card 3 -->
            <div class="card">
                <h3>⚙️ TOOL AUTOMATION</h3>
                <ul>
                    <li>Tool rải link, bot rải link tự động</li>
                    <li>Hệ thống CODE & NHIỆM VỤ TYM 💜</li>
                    <li>Tự động hóa MMO tối ưu chi phí</li>
                </ul>
            </div>

            <!-- Card 4 -->
            <div class="card">
                <h3>💳 SET UP CẤU HÌNH</h3>
                <ul>
                    <li>Thay đổi Tên Nhân Vật theo brand</li>
                    <li>Tích hợp STK nhận tiền tự động</li>
                    <li>Hệ thống vận hành 24/7 ổn định</li>
                </ul>
            </div>

        </section>

        <!-- Phần thời gian / gói dịch vụ -->
        <section class="pricing-section">
            <h2>⏱️ THỜI GIAN TRIỂN KHAI LINH HOẠT</h2>
            <div class="price-container">
                <div class="price-box">
                    <h4>GÓI TUẦN</h4>
                    <div class="duration">Chạy TEST / Ngắn hạn</div>
                    <p style="color: var(--text-muted);">Phù hợp cho các chiến dịch kéo mem cấp tốc, test hiệu quả bot.</p>
                </div>
                <div class="price-box active">
                    <h4>GÓI THÁNG</h4>
                    <div class="duration">Chạy DUY TRÌ / Dài hạn</div>
                    <p style="color: var(--text-muted);">Ưu đãi giá cực tốt. Hỗ trợ kỹ thuật và bảo trì miễn phí suốt tháng.</p>
                </div>
            </div>
        </section>

        <!-- Chân trang -->
        <footer>
            <p>© 2026 Telegram Automation Service. Hệ thống vận hành tự động.</p>
            <p style="margin-top: 10px; color: var(--secondary-neon);">Contact: @TenVipQua</p>
        </footer>

    </div>

</body>
</html>